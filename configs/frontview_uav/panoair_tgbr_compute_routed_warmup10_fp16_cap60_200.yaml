inherit_from: "configs/frontview_uav/panoair_tgbr_compute_routed_warmup10_fp16_200.yaml"

Dataset:
  name: "PanoAir-tgbr-compute-routed-warmup10-fp16-cap60-200"

Testset:
  name: "PanoAir-tgbr-compute-routed-warmup10-fp16-cap60-200"

StreamingAppearanceLOD:
  max_target_fraction: 0.60
